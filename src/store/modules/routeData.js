const state = {
  routeData: {}
}

const getters = {
}

const mutations = {
  storageData (state, data) {
    state.routeData = data
  }
}

const actions = {

}

export default {
  state,
  getters,
  mutations,
  actions
}
